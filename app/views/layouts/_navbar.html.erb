<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'LHU language', root_url, class: 'navbar-brand' %>
    </div>

    <div class="navbar-collapse collapse">

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li>
            <%= link_to 'Notifications', user_notifications_path(current_user), remote: true %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Menu
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to user_notifications_path(current_user) do%>
                  Notification&nbsp;
                  <% unless current_user.notifications.uncheck.blank? %>
                    <span class="badge"><%= current_user.notifications.uncheck.count %></span>
                  <% end %>
                <% end %>
              </li>
              <li><%= link_to 'Create demand', new_demand_path %></li>
              <li><%= link_to 'Profile', current_user %></li>
              <li class="divider"></li>
              <li><%= link_to 'Log out', logout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <%# nav_link 'Sign up', signup_path %>
          <%# nav_link 'Log in', login_path %>
          <li><%= link_to 'Sign up', signup_path %></li>
          <li><%= link_to 'Log in', login_path %></li>
        <% end %>
      </ul>

    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</div>