source 'https://rubygems.org'


gem 'rails', '4.1.6'
gem 'sass-rails', '~> 4.0.3'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'jquery-rails'
gem 'jquery-ui-rails'
gem 'turbolinks'
gem 'jquery-turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0',          group: :doc
gem 'bcrypt'
gem 'bootstrap-sass'

group :development do
  gem 'sqlite3'

  # debugging in Ruby 2
  gem 'byebug'

  # rails Console on the Browser.
  gem 'web-console'
  gem 'spring'
end

group :test do

  # create customizable MiniTest output formats.
  gem 'minitest-reporters'

  # allows you to take advantage of the Rails.backtrace_cleaner when using MiniTest
  gem 'mini_backtrace'

  # automatically run your tests
  gem 'guard-minitest'
end

group :production do
  gem 'pg'
  gem 'rails_12factor'
end